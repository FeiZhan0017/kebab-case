# kebab-case

This repository contains a simple terminal-based Tetris game written in Python.

## Running the game

```bash
python tetris.py
```

Press arrow keys to move the falling pieces, the up key to rotate, and `q` to quit.

## Self-test

```bash
python tetris.py --test
```
